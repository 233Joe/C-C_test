#include <stdio.h>
int main(){
	if(1){

		int a = 40,c = 212;
		int price = (22-8)*a-10+c/2;
		printf("%d\n",price);
	
		
	}
	
	if(1){
		int x = 0,y = 0; 
		scanf("%d",&x);
		if(x > 0)
			y = -1;
		
		else if(x < 0)
			y = 1;
		printf("输入:");
		printf("%d\n",x);
		
		printf("输出:");
		printf("%d",y);
		
		return 0;
	}
}
